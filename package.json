{
  "name": "iquiz-api",
  "version": "1.0.0",
  "description": "iQuiz backend API",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production nodemon server",
    "dev": "nodemon server"
  },
  "author": "Greg Pan",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  }
}
